import { Batch } from './types/batch.js';
import { Room } from './types/room.js';
// batch days room

// batch->section->group->subject->teacher
// exam rooom & day -> timeAtom

function Generate(batch: Batch,days: Date[],rooms: Room[]){

}